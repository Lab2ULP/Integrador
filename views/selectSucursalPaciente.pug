extends layoutCliente

block content
  h1 Seleccione una sucursal para consultar disponibilidad de turnos
  form(action="/pacientes/principal", method="GET")
    label(for="sucursal") Sucursal:
    select(name="sucursal", ID="sucursal")
      each sucursal in sucursales
        option(value=sucursal.ID)= sucursal.nombre

    button(type="submit" style="display: block; margin: 20px auto 0 auto;") Seleccionar